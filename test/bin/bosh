#!/usr/bin/env bash

if [ "$1" != "target" ] && ([ "$1" != "sync" ] || [ "$2" != "blobs" ]); then
  >&2 echo "Error: Expect nothing but '$BASH_SOURCE sync blobs' or '$BASH_SOURCE target'"
  exit 1
fi
